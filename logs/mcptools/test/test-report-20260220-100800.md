# MCP Test Report: che-zotero-mcp
Generated: 2026-02-20 10:08

## 測試摘要
- 總 Tools: 15
- 可測試（此 session）: 7（舊 binary，新增 8 個 tools 需重啟 session）
- 測試通過: 5
- 測試失敗: 0
- 條件未滿足（無法測試）: 2
- 待測（需重啟 session）: 8
- 覆蓋率: 5/7 = 71%（可測試範圍內），5/15 = 33%（全部）

## 測試環境
- Binary: `/Users/che/bin/CheZoteroMCP`（已部署 v1.0.0，但 session 仍連舊進程）
- Xcode: 26.2
- Swift: 6.2.3
- 編譯: `swift build -c release` ✅ 通過

## 測試結果

### 讀取類 (4/4 ✅)
| Tool | 結果 | 備註 |
|------|------|------|
| `zotero_get_collections` | ✅ | 回傳 2 個 collections |
| `zotero_get_tags` | ✅ | 回傳 350 個 tags |
| `zotero_get_recent` | ✅ | 回傳 3 筆（limit=3）|
| `zotero_get_metadata` | ✅ | key=MXVCWI5B，完整 metadata + abstract |

### 搜尋類 (1/1 ✅)
| Tool | 結果 | 備註 |
|------|------|------|
| `zotero_search` | ✅ | query="mindset" 回傳 3 筆 |

### Semantic Search (0/2 ⚠️ 條件未滿足)
| Tool | 結果 | 備註 |
|------|------|------|
| `zotero_build_index` | ⏭️ 跳過 | 需下載 1.5GB 模型，耗時過長 |
| `zotero_semantic_search` | ⚠️ | 正確回傳 "index empty" 錯誤（預期行為）|

### 新增 Enhanced Zotero Tools (0/3 🔄 待測)
| Tool | 結果 | 備註 |
|------|------|------|
| `zotero_get_items_in_collection` | 🔄 待測 | 新 binary 未載入，需重啟 session |
| `zotero_search_by_doi` | 🔄 待測 | 新 binary 未載入，需重啟 session |
| `zotero_get_attachments` | 🔄 待測 | 新 binary 未載入，需重啟 session |

### 新增 Academic Search Tools (0/5 🔄 待測)
| Tool | 結果 | 備註 |
|------|------|------|
| `academic_search` | 🔄 待測 | 新 binary 未載入，需重啟 session |
| `academic_get_paper` | 🔄 待測 | 新 binary 未載入，需重啟 session |
| `academic_get_citations` | 🔄 待測 | 新 binary 未載入，需重啟 session |
| `academic_get_references` | 🔄 待測 | 新 binary 未載入，需重啟 session |
| `academic_search_author` | 🔄 待測 | 新 binary 未載入，需重啟 session |

## 清理狀態
- 測試資料已全部清理: ✅（所有 tools 皆為唯讀，無需清理）

## 問題與備註

### 1. MCP SDK 0.11.0 無法使用
- **原因**: MCP SDK 0.11.0 新增 swift-nio 依賴，swift-nio 2.95.0 與 Xcode 26.2 SDK headers 不相容
- **處理**: Package.swift 使用 `.upToNextMinor(from: "0.10.2")` 鎖定 0.10.x
- **影響**: 無，MCP SDK 0.10.2 功能完整

### 2. 新 Tools 無法在當前 session 測試
- **原因**: `claude mcp remove/add` 只更新 config 檔，不重啟當前 session 的 MCP 進程
- **解決**: 重啟 Claude Code session 後重新執行測試

### 3. Semantic Search 需要手動驗證
- `zotero_build_index` 首次需下載 ~1.5GB BAAI/bge-m3 模型
- 建議在非測試情境下單獨執行，確認：
  1. 模型下載成功
  2. Index 建立完成
  3. 持久化到 `~/.che-zotero-mcp/embeddings.sqlite`
  4. 重啟後 `zotero_semantic_search` 可直接使用

## 後續行動
1. **重啟 session** 後重新測試全部 15 個 tools
2. 特別驗證 `academic_search` 搜 "HTKS-R" 能回傳結果
3. 驗證 Embedding 持久化完整生命週期
